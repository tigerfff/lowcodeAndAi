# 标准列表页模板 (Standard List)

## 📋 模板概述

**模板 ID**: `standard-list`  
**版本**: 1.0.0  
**适用场景**: 数据查询、管理列表、CRUD 操作等标准列表页面

这是最常用的页面模板，包含搜索栏、数据表格和分页器，适合大部分后台管理系统的列表页开发。

## 🎨 页面结构

```
┌─────────────────────────────────────┐
│  面包屑导航 (h-page-header)          │
├─────────────────────────────────────┤
│  搜索栏 (h-page-search)              │
│  ┌───────────┬───────────┬────────┐ │
│  │ 输入框    │ 下拉选择  │ 日期   │ │
│  └───────────┴───────────┴────────┘ │
│  [查询] [重置]                       │
├─────────────────────────────────────┤
│  数据表格 (el-table)                 │
│  ┌────┬─────┬─────┬─────┬────────┐ │
│  │列1 │列2  │列3  │列4  │ 操作   │ │
│  ├────┼─────┼─────┼─────┼────────┤ │
│  │... │...  │...  │...  │ 详情.. │ │
│  └────┴─────┴─────┴─────┴────────┘ │
├─────────────────────────────────────┤
│  分页器 (el-pagination)              │
│  共 100 条 [10] [上一页][1][下一页] │
└─────────────────────────────────────┘
```

## 📦 包含组件

- `h-page-container` - 页面容器
- `h-page-header` - 页面头部（面包屑）
- `h-page-content` - 内容区域
- `h-page-search` - 搜索栏容器
- `h-page-search-item` - 搜索项
- `h-page-table` - 表格容器
- `el-table` - 数据表格
- `el-table-column` - 表格列
- `el-pagination` - 分页器

## 🤖 AI 推断任务

AI 会根据提供的 API 文档自动推断以下内容：

1. **表格列配置**
   - 字段名（prop）
   - 显示标签（label）
   - 列宽（width）
   - 数据类型（text/number/datetime/tag）
   - 格式化函数（formatter）

2. **搜索字段配置**
   - 字段名（prop）
   - 控件类型（input/select/daterange）
   - 默认值（defaultValue）
   - 选项数据源（options）

3. **数据映射**
   - 列表数据路径（如 `data.rows`）
   - 总数路径（如 `data.total`）
   - 分页参数字段（pageNo/pageSize 或其变体）

## 📝 配置示例

### 最小配置

```json
{
  "pageName": "UserList",
  "breadcrumb": ["首页", "用户列表"],
  "api": {
    "url": "/api/user/list",
    "method": "POST"
  },
  "columns": [
    { "prop": "userName", "label": "用户名" },
    { "prop": "phone", "label": "手机号" }
  ],
  "dataMapping": {
    "dataPath": "data.rows",
    "totalPath": "data.total"
  }
}
```

### 完整配置

```json
{
  "pageName": "UserList",
  "description": "用户管理列表页",
  "breadcrumb": ["首页", "用户管理", "用户列表"],
  "api": {
    "url": "/api/user/list",
    "method": "POST",
    "importPath": "@/api/user"
  },
  "searchFields": [
    {
      "prop": "userName",
      "label": "用户名",
      "type": "input",
      "placeholder": "请输入用户名",
      "defaultValue": ""
    },
    {
      "prop": "status",
      "label": "状态",
      "type": "select",
      "defaultValue": "",
      "options": {
        "static": [
          { "value": "1", "label": "启用" },
          { "value": "0", "label": "禁用" }
        ]
      }
    },
    {
      "prop": "createTime",
      "label": "创建时间",
      "type": "daterange",
      "defaultValue": []
    }
  ],
  "columns": [
    { "prop": "userName", "label": "用户名", "width": "150" },
    { "prop": "phone", "label": "手机号", "width": "120" },
    { "prop": "email", "label": "邮箱", "width": "200" },
    { "prop": "createTime", "label": "创建时间", "width": "180", "type": "datetime" },
    {
      "prop": "status",
      "label": "状态",
      "width": "100",
      "type": "custom",
      "customRender": "<el-tag :type=\"scope.row.status === '1' ? 'success' : 'danger'\">{{ scope.row.status === '1' ? '启用' : '禁用' }}</el-tag>"
    }
  ],
  "dataMapping": {
    "dataPath": "data.rows",
    "totalPath": "data.total",
    "pageNoField": "pageNo",
    "pageSizeField": "pageSize"
  },
  "operationColumn": {
    "label": "操作",
    "width": "200",
    "fixed": "right",
    "buttons": [
      { "label": "详情", "action": "handleDetail" },
      { "label": "编辑", "action": "handleEdit" },
      { "label": "删除", "action": "handleDelete" }
    ]
  }
}
```

## 🔧 搜索字段类型

### input - 输入框
```json
{
  "prop": "keyword",
  "label": "关键字",
  "type": "input",
  "placeholder": "请输入关键字",
  "defaultValue": ""
}
```

### select - 下拉选择
```json
{
  "prop": "status",
  "label": "状态",
  "type": "select",
  "defaultValue": "",
  "options": {
    "static": [
      { "value": "1", "label": "启用" },
      { "value": "0", "label": "禁用" }
    ]
  }
}
```

### daterange - 日期范围
```json
{
  "prop": "timeRange",
  "label": "时间范围",
  "type": "daterange",
  "defaultValue": []
}
```

### date - 单个日期
```json
{
  "prop": "createDate",
  "label": "创建日期",
  "type": "date",
  "defaultValue": ""
}
```

## 📊 表格列类型

### text - 普通文本（默认）
```json
{ "prop": "userName", "label": "用户名" }
```

### datetime - 日期时间
```json
{ "prop": "createTime", "label": "创建时间", "type": "datetime" }
```

### custom - 自定义渲染
```json
{
  "prop": "status",
  "label": "状态",
  "type": "custom",
  "customRender": "<el-tag>{{ scope.row.status }}</el-tag>"
}
```

## 🔄 数据流配置

### 数据路径映射

模板支持多种 API 响应结构，会按优先级自动匹配：

**列表数据路径** (dataPath):
- `data.rows`
- `data.items`
- `data.list`
- `data.records`
- `rows`
- `items`

**总数路径** (totalPath):
- `data.total`
- `data.totalRecord`
- `data.totalCount`
- `total`

**分页参数** (pagination):
- `pageNo` / `pageSize`
- `current` / `size`
- `page` / `limit`

### 函数式映射（高级）

如果 API 响应结构复杂，可以使用函数式映射：

```json
{
  "dataMapping": {
    "dataPath": "(json) => json.result.data.userList",
    "totalPath": "(json) => json.result.pagination.total"
  }
}
```

## 🎯 生成的代码特性

生成的 Vue2 代码具有以下特性：

1. ✅ **完整的生命周期** - mounted 时自动加载数据
2. ✅ **搜索功能** - 支持查询、重置
3. ✅ **分页功能** - 页码切换、每页数量切换
4. ✅ **错误处理** - try-catch 捕获异常
5. ✅ **代码格式化** - 符合 ESLint 规范
6. ✅ **注释完整** - 每个方法都有说明

## 📸 预览截图

由于这是模板定义，实际页面外观取决于生成时的配置。

典型的页面效果：
- 顶部：面包屑导航
- 搜索区：2-6 个搜索字段，支持高低频切换
- 表格区：可配置任意列数，支持固定列、自定义渲染
- 底部：分页器，显示总数、页码选择器

## 🚀 使用流程

1. 用户选择此模板
2. 输入/粘贴 API 文档（OpenAPI 或样例 JSON）
3. AI 自动推断配置（列、搜索字段、数据映射）
4. 用户在确认面板微调配置
5. 生成 Vue2 代码
6. 复制代码到项目中使用

## 📌 注意事项

1. **API 格式要求**
   - 推荐使用 POST 方法（支持复杂搜索条件）
   - 响应数据应包含列表数组和总数

2. **搜索字段限制**
   - 建议不超过 6 个常用搜索字段
   - 更多字段可设置为高低频过滤

3. **表格列优化**
   - 建议显示列数不超过 8 个
   - 固定列会影响性能，谨慎使用

4. **分页参数**
   - 确保前后端分页参数命名一致
   - 支持常见的分页参数变体

## 🔗 相关文件

- `template.json` - 模板元数据定义
- `page.vue.hbs` - Handlebars 模板文件
- `../../manifests/hui-components.json` - 组件库元数据

