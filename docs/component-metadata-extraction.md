# HUI 组件元数据提取总结

## 📋 完成情况

### ✅ 已完成的文件

1. **`manifests/hui-components.schema.json`** - JSON Schema 定义
   - 定义了组件元数据的标准结构
   - 包含必需字段和可选字段的约束
   - 支持组件分类、属性、事件、插槽、方法等完整信息

2. **`manifests/hui-components.json`** - 组件元数据库
   - 已提取 **22 个核心组件**的完整元数据
   - 覆盖 5 个主要分类（layout、form、list、navigation、data）
   - 包含详细的使用约束和依赖关系

3. **`scripts/validate-components.js`** - 验证脚本
   - JSON Schema 自动校验
   - 必需组件完整性检查
   - 特殊标注规则验证
   - 统计信息输出

4. **`package.json`** - 添加验证脚本命令
   - `npm run validate:components` - 运行验证

## 📊 提取的组件清单

### 布局组件 (10 个) - **最重要**

这些是典型页面模板的核心组件，后续插入 slot 都依靠它们：

1. **`h-page-container`** ⭐⭐⭐
   - 最外层容器，所有页面必用
   - 提供 `pageHeader` 插槽用于放置页头
   - 提供 `on-scroll` 事件监听页面滚动

2. **`h-page-content`** ⭐⭐⭐
   - 主内容区域容器
   - 支持 flex 布局，可控制内部元素排列方向
   - 通常作为 h-page-container 的子组件

3. **`h-page-header`** ⭐⭐⭐
   - 页面头部，包含面包屑、标题、返回按钮
   - 必须通过 `slot="pageHeader"` 放入 h-page-container
   - 提供 `pageHeaderAction` 插槽用于右侧操作按钮

4. **`h-page-action`** ⭐⭐
   - 操作栏，承载页面操作按钮
   - 支持左右两侧插槽
   - 可与 h-page-search 联动控制搜索框显隐

5. **`h-page-search`** ⭐⭐⭐
   - 搜索栏容器，用于筛选数据
   - 必须配合 h-page-search-item 使用
   - 支持高低频搜索、响应式布局
   - 提供表单验证、重置等方法

6. **`h-page-search-item`** ⭐⭐⭐
   - 搜索项，定义单个搜索字段
   - 必须在 h-page-search 内使用
   - 支持高低频过滤（通过 `show` 属性）

7. **`h-page-sidebar`** ⭐
   - 侧边栏，用于二级导航
   - 支持列表和树形两种模式
   - 支持图钉模式和内置滚动条

8. **`h-page-table`** ⭐⭐
   - 表格内容容器
   - 支持表头、滚动条、分页栏的固定模式
   - full 模式下占据剩余空间

9. **`h-page-content-table`** ⭐⭐
   - 一体化表格组件
   - 集成搜索栏、操作栏、表格、分页
   - 适合快速搭建标准列表页

10. **`h-layout`** ⭐
    - 布局容器，用于包裹侧边栏

### 列表组件 (3 个)

1. **`h-paged-table`** ⭐⭐⭐
   - 带分页的表格组件
   - **支持函数式映射数据路径**（重要特性）
   - 如 `:data="(json) => json.data.items"`
   - 可通过 url 或 fetch 进行异步加载

2. **`el-table`** ⭐⭐⭐
   - 基础表格组件
   - 支持排序、筛选、选择、展开等功能
   - force-scroll 用于美化滚动条
   - 不再支持内置分页，需用 h-paged-table

3. **`el-table-column`** ⭐⭐⭐
   - 表格列定义
   - 支持自定义内容和表头
   - type 可选 selection/radio/index/expand

### 表单组件 (7 个)

1. **`el-form`** ⭐⭐⭐
   - 表单容器，必须传入 model
   - 支持验证、重置等功能
   - 必须配合 el-form-item 使用

2. **`el-form-item`** ⭐⭐⭐
   - 表单项，包裹表单控件
   - 提供标签、验证功能
   - prop 属性对应 model 字段

3. **`el-input`** ⭐⭐⭐
   - 输入框，支持 v-model
   - type 可选 text/textarea/password

4. **`el-select`** ⭐⭐⭐
   - 选择器，支持 v-model
   - 支持单选、多选、搜索、分组
   - clear 用于单选清空，clearable 用于多选

5. **`el-option`** ⭐⭐⭐
   - 选择器选项
   - 必须在 el-select 内使用

6. **`el-option-group`** ⭐⭐
   - 选择器分组
   - 内部包含 el-option

7. **`el-button`** ⭐⭐⭐
   - 按钮组件
   - type 可选 primary/success/warning/danger/info/text/link/iconButton

### 导航组件 (1 个)

1. **`el-pagination`** ⭐⭐⭐
   - 分页组件
   - layout 控制显示内容
   - 支持 v-model 绑定当前页

### 数据组件 (1 个)

1. **`el-tree`** ⭐⭐
   - 树形控件
   - 支持选择、展开、拖拽、懒加载
   - props 属性配置字段映射

## 🎯 元数据结构说明

每个组件包含以下信息：

```javascript
{
  "name": "组件名称",              // 如 h-page-container
  "category": "分类",             // layout/form/list/navigation/data/feedback/other
  "description": "组件描述",
  "requiredProps": [],           // 必需的 props 数组
  "commonProps": {},             // 常用 props 及默认值对象
  "events": [],                  // 支持的事件数组
  "vModel": "绑定属性名或null",   // v-model 支持
  "slots": {},                   // 插槽定义对象
  "methods": [],                 // 暴露的方法数组
  "specialNote": "特殊说明",      // 重要信息、约束、依赖关系
  "usageConstraints": [],        // 使用约束列表
  "dependencies": []             // 依赖的其他组件
}
```

## 🔍 关键特性标注

### 1. 函数式映射 (h-paged-table)
```javascript
"specialNote": "支持函数式映射数据路径，如 :data='(json) => json.data.items' 和 :total='(json) => json.data.total'"
```
这是 AI 推断数据映射时的重要依据。

### 2. 插槽依赖关系
```javascript
// h-page-header 必须通过 slot 放入父组件
"specialNote": "必须通过 slot='pageHeader' 放入 h-page-container 中"
"dependencies": ["h-page-container"]
```

### 3. 组件嵌套约束
```javascript
// h-page-search-item 必须在父组件内使用
"usageConstraints": [
  "必须在 h-page-search 内使用",
  "必须设置 prop 属性对应 model 中的字段"
]
```

### 4. 特殊属性说明
```javascript
// el-table 的 force-scroll 属性
"specialNote": "force-scroll 属性用于高度自适应时强制美化滚动条"
"usageConstraints": [
  "force-scroll 需要外部容器有指定高度"
]
```

## 🚀 验证脚本功能

运行 `npm run validate:components` 会执行：

1. ✅ **JSON Schema 校验** - 确保数据结构正确
2. ✅ **必需组件检查** - 确保 22 个核心组件都已提取
3. ✅ **特殊标注检查** - 确保关键信息已标注：
   - h-page-container 的最外层容器说明
   - h-page-header 的 slot 使用说明
   - h-page-search 的 model 必需属性
   - h-paged-table 的函数式映射说明
   - el-table 的 force-scroll 说明
4. 📊 **统计信息输出** - 组件数量、分类分布、特性统计

## 📝 使用场景

### AI 推断时的应用

1. **组件选择约束**
   - 通过 category 和 description 选择合适组件
   - 通过 dependencies 确保依赖组件已存在

2. **属性推断**
   - requiredProps：必须生成的属性
   - commonProps：提供默认值
   - vModel：自动生成双向绑定

3. **数据映射推断**
   - 根据 specialNote 中的"函数式映射"提示
   - 为 h-paged-table 生成数据路径函数

4. **插槽填充**
   - 根据 slots 信息生成插槽内容
   - 根据 dependencies 确保父子组件关系正确

5. **事件绑定**
   - 根据 events 列表生成常用事件处理

### 代码生成器应用

1. **结构验证**
   - 检查组件嵌套关系是否符合 dependencies
   - 检查必需属性是否都已配置

2. **默认值填充**
   - 从 commonProps 获取默认值
   - 生成完整的组件配置

3. **类型检查**
   - 验证 props 类型是否正确
   - 验证 v-model 绑定是否合法

## 🎓 典型页面模板组件组合

### 标准列表页
```
h-page-container
  └─ h-page-header (slot="pageHeader")
      └─ 面包屑、标题
  └─ h-page-content
      ├─ h-page-action
      │   └─ 操作按钮
      ├─ h-page-search
      │   └─ h-page-search-item × N
      │       └─ 表单控件 (el-input、el-select 等)
      └─ h-paged-table
          └─ el-table-column × N
```

### 快速列表页（使用一体化组件）
```
h-page-container
  └─ h-page-header (slot="pageHeader")
  └─ h-page-content-table
      ├─ hPageAction (slot)
      ├─ hPageSearch (slot)
      ├─ hPageTable (slot)
      └─ pagination (slot)
```

### 表单页
```
h-page-container
  └─ h-page-header (slot="pageHeader")
  └─ h-page-content
      └─ el-form
          └─ el-form-item × N
              └─ 表单控件
```

### 侧边栏页面
```
h-page-container
  └─ h-page-header (slot="pageHeader")
  └─ h-layout
      ├─ h-page-sidebar
      │   └─ el-tree 或列表
      └─ h-page-content
          └─ 内容区域
```

## 🎯 下一步工作

根据 todoList.md，后续任务：

1. ✅ 1.2.1 定义 hui-components.schema.json
2. ✅ 1.2.2 提取 h-paged-table 元数据
3. ✅ 1.2.3 提取其余 P0 组件（22 个）
4. ✅ 1.2.4 编写验证脚本并通过校验

**已全部完成！可以继续下一阶段的工作。**

## 💡 重要提示

1. **布局组件是核心**：h-page-container、h-page-header、h-page-content、h-page-search 是构建典型页面的基础
2. **函数式映射是关键特性**：h-paged-table 的函数式映射让 AI 可以灵活处理不同的 API 响应结构
3. **依赖关系很重要**：某些组件必须在特定父组件内使用，生成代码时需要验证
4. **插槽命名规范**：注意某些组件的插槽有特殊命名（如 pageHeader、pageSearchAction）
5. **v-model 支持有限**：只有 el-input、el-select 等表单控件支持 v-model

## 📖 参考文档

- 原始文档位置：`hui/*.md`
- Schema 定义：`manifests/hui-components.schema.json`
- 元数据文件：`manifests/hui-components.json`
- 验证脚本：`scripts/validate-components.js`
- 产品文档：`产品文档.md`
- 技术方案：`技术方案.md`

